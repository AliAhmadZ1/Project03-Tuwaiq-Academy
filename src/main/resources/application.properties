spring.application.name=Project03_TuwaiqAcademy

server.port=5000
# database url
spring.datasource.url=jdbc:mysql://database-1.cxi22w4is137.eu-north-1.rds.amazonaws.com/secured_bank
# alli
spring.datasource.username=admin

spring.datasource.password=Lsto3695Ali

# run hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# show sql commands
spring.jpa.show-sql=true

# ddl (create, alter, truncate)
spring.jpa.generate-ddl=true

# drop all then create all tables or dbs
# update is necessary for relations
spring.jpa.hibernate.ddl-auto=update

# show all errors and trace it
server.error.include-message=always
server.error.include-stacktrace=always